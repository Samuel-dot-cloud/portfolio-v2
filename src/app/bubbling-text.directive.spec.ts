import { BubblingTextDirective } from './bubbling-text.directive';

describe('BubblingTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BubblingTextDirective();
    expect(directive).toBeTruthy();
  });
});
